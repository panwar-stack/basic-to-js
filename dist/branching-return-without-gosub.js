"use strict";

// Generated by basic-js-compiler 0.1.0.
// Source: test/fixtures/branching-return-without-gosub.bas
// Compiler metadata: deterministic, timestamp-free output.

function main() {
  let pc = 0;
  const vars = Object.create(null);
  const arrays = Object.create(null);
  const gosubStack = [];
  const forStack = [];
  const labels = Object.freeze({ "10": 0, "20": 1 });
  const statements = new Array(2);

  function runtimeError(code, message) {
    console.error(`BASIC runtime error ${code}: ${message}`);
    process.exit(1);
  }

  while (pc < statements.length) {
    switch (pc) {
      case 0:
        // BASIC line 1: ReturnStatement
        if (gosubStack.length === 0) {
          runtimeError("BASRT002", "RETURN without GOSUB");
        }
        pc = gosubStack.pop();
        break;
      case 1:
        // BASIC line 2: EndStatement
        pc = statements.length;
        break;
      default:
        pc = statements.length;
        break;
    }
  }
}

main();
