"use strict";

// Generated by basic-js-compiler 0.1.0.
// Source: test/fixtures/codegen-unary-exponent.bas
// Compiler metadata: deterministic, timestamp-free output.

function main() {
  let pc = 0;
  const vars = Object.create(null);
  const arrays = Object.create(null);
  const gosubStack = [];
  const forStack = [];
  const labels = Object.freeze({ "10": 0 });
  const statements = new Array(1);

  function printValue(value) {
    process.stdout.write(String(value));
  }

  while (pc < statements.length) {
    switch (pc) {
      case 0:
        // BASIC line 1: PrintStatement
        printValue(-(2 ** 2));
        process.stdout.write("\n");
        pc += 1;
        break;
      default:
        pc = statements.length;
        break;
    }
  }
}

main();
